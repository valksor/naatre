version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - exhaustive
    - govet
    - ineffassign
    - nilerr
    - staticcheck

formatters:
  enable:
    - gofmt
    - goimports

run:
  timeout: 5m
